# Environment variables for BioMechanics_Microprocessor
# Loaded automatically by direnv when .envrc is allowed

# Debug mode (0=off, 1=on)
KINEINTRA_DEBUG=0

# Virtual device defaults
KINEINTRA_VIRTUAL_PORT=8888
KINEINTRA_VIRTUAL_HOST=127.0.0.1

# Serial connection defaults
# KINEINTRA_DEFAULT_PORT=/dev/ttyUSB0
# KINEINTRA_BAUDRATE=115200

# Logging level (DEBUG, INFO, WARNING, ERROR)
KINEINTRA_LOG_LEVEL=INFO

# Add your custom environment variables below
